name: calculator
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Tambahkan dua baris ini
  provider: ^6.1.2 
  math_expressions: ^2.5.0
  cupertino_icons: ^1.0.2      # Untuk mengevaluasi ekspresi matematika

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
